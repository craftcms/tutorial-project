{% extends '_layout' %}

{% set posts = craft.entries().section('blog').topics(category).all() %}

{% block content %}
  <h1>Topic: {{ category.title }}</h1>

  {% include 'blog/_feed' %}
{% endblock %}